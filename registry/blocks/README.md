# Blocks

Future complete UI flows live here. A block combines multiple UI components into a task-oriented screen such as a learning completion flow, dashboard, or onboarding step. Register each block as `registry:block` in the root registry when added.
